图名：问题1温度场与水分浓度场

用途：展示固定半径、附件2物性下，径向位置与时间共同变化的温度和水分浓度。
面板A为温度（°C），面板B为水分浓度（kg/kg）；横轴为距中心径向距离（cm），纵轴为时间（min）。
颜色按反向连续顺序色标编码数值：低温/低浓度为浅色，高温/高浓度为深色；温度最高值为深紫色，不使用黑色；色条单位位于顶部。图中使用 256 级矢量等值填色，仅为显示量化，源数据未平滑。
